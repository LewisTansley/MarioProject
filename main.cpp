#include <stdio.h>
#include "source.h"

int main(int argc, char* args[]) {

	Game* game = new Game(argc, args); // Creates initial game instance

	return 0; // exit with error code 0 if successful

}
